Read the engspec prompt and format spec for your instructions:

1. Read `engspec_format.md` in this repository for the `.engspec` format specification.
2. Read `engspec_prompt.md` in this repository for the complete workflow.

Then execute the workflow with the following user input:

$ARGUMENTS

If no arguments are provided, ask the user for:
- A repo path or git URL
- Whether they want full, partial (which files), or incremental mode
- An existing engspec package path (for incremental mode)
